This project is a raycaster

for launching the project use make file commande "make" 

set executable followed by map, example : ./Cub3D ./maps/base_map.cub 

use 'R' to display or remove minimap 

if not all the .o files dissepear , use "find . -type f -name "*.o" -exec rm -f {} +" inside srcs folder to remove them